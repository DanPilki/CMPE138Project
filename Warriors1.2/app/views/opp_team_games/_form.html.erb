<%= form_for(opp_team_game) do |f| %>
  <% if opp_team_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opp_team_game.errors.count, "error") %> prohibited this opp_team_game from being saved:</h2>

      <ul>
      <% opp_team_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :opp_teams_id %>
    <%= f.text_field :opp_teams_id %>
  </div>

  <div class="field">
    <%= f.label :games_id %>
    <%= f.text_field :games_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
