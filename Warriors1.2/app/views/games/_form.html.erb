<%= form_for(game) do |f| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :home_away %>
    <%= f.text_field :home_away %>
  </div>

  <div class="field">
    <%= f.label :occasion %>
    <%= f.date_select :occasion %>
  </div>

  <div class="field">
    <%= f.label :results %>
    <%= f.text_field :results %>
  </div>

  <div class="field">
    <%= f.label :score %>
    <%= f.text_field :score %>
  </div>

  <div class="field">
    <%= f.label :opponents %>
    <%= f.text_field :opponents %>
  </div>

  <div class="field">
    <%= f.label :win_lose_stat %>
    <%= f.text_field :win_lose_stat %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
