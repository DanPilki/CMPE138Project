<%= form_for(players_play) do |f| %>
  <% if players_play.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(players_play.errors.count, "error") %> prohibited this players_play from being saved:</h2>

      <ul>
      <% players_play.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :players_id %>
    <%= f.text_field :players_id %>
  </div>

  <div class="field">
    <%= f.label :games_id %>
    <%= f.text_field :games_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
